@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/Logout">
        <button type="submit" class="btn btn-link">Выйти</button>
    </form>
}
else
{
    <a class="btn" asp-page="/Account/Login">Войти</a>
}